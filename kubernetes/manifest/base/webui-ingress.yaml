apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: iagentic-ingress
  namespace: iagentic
  #annotations:
    # Use appropriate annotations for your Ingress controller, e.g., for NGINX:
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: iagentic.minikube.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: iagentic-service
            port:
              number: 8080
